apiVersion: v1
kind: Service
metadata:
  name: test-service
spec:
  selector:
    app: nginx # Ensure this matches your pod's labels
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
  loadBalancerIP: 192.168.100.200 # Optional: forces MetalLB to use this IP